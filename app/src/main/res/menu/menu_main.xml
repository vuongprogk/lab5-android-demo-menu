<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!--    app:showAsAction="always" always show in action bar -->
    <!--    title only show not as Actions except for configuration-->
    <item
        android:id="@+id/mnuSearch"
        android:icon="@drawable/ic_search_black_24dp"
        android:title="@string/search"
        app:actionViewClass="android.widget.SearchView"
        app:showAsAction="always" />
    <item
        android:id="@+id/mnuAlert"
        android:icon="@drawable/ic_alert"
        android:title="@string/alert"
        app:showAsAction="always" />
    <item
        android:id="@+id/mnuAbout"
        android:icon="@drawable/ic_about"
        android:title="@string/about"
        app:showAsAction="never" />
    <item
        android:id="@+id/mnuExit"
        android:icon="@drawable/ic_exit"
        android:title="@string/exit"
        app:showAsAction="never" />

</menu>